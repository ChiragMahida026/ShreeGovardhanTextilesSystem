<Page x:Class="ShreeGovardhanTextilesSystem.Pages.Popup"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShreeGovardhanTextilesSystem.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Popup">

    <Border CornerRadius="5" Background="{DynamicResource SecundaryBackgroundColor}">

        <Grid>
            <StackPanel Width="400" Orientation="Horizontal">
                <Label>
                    my info
                </Label>
                <Label>
                    jsakjs
                </Label>
            </StackPanel>
            <StackPanel>
                <TextBlock>
                    More 
                </TextBlock>
            </StackPanel>
            <Button x:Name="TestingButton" Width="200" Height="20" HorizontalAlignment="Center"  VerticalAlignment="Bottom" > 
               Exit
            </Button>
        </Grid>
    </Border>
</Page>
